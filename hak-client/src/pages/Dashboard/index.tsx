import { Table } from '@mui/material';
// import { useQuery } from 'react-query';

export const Dashboard = () => {
  // const { data} = useQuery('/map/oil-spills')
  // console.log(data)
  return <Table></Table>;
};
